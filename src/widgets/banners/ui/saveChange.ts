import { banners } from './BannersSection.vue'

export function saveChange() {
  const one = banners.value[0]

  zdfcszdcweaasa
  console.log()
  //zax
  // bannersFromServer.forEach((item) => {
  //   console.log(compareObjects(one, item))
  // })
  // const filterBanners = computedItems.value.filter((bannerLocal: Banner) => {
  //   bannersFromServer.forEach((bannerServer: Banner) => {
  //     console.log(compareObjects(bannerServer, bannerLocal))
  //   })
  // })
  // console.log(computedItems.value, bannersFromServer)
}
